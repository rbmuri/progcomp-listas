#include <iostream>
#include <string>
#include <queue>
#include <vector>
#include <map>
#include <set>
#include <queue>
 
//???? (enunciado confuso)

using namespace std;

int main(){
    int r, c, k;
    cin >> r >> c >> k;
    while (k){
        char t[r][c];
        int mtx[r][c];
        for (int i=0; i<r; i++){
            for (int j=0; j<c; j++){
                cin >> t[i][j];
            }
        }
        for (int i=0; i<r; i++){
            for (int j=0; j<c; j++){
                if (t[i][j]=='.'){
                    
                }
            }
        }
        for (int a=0; a<r; a++){
            for (int b=0; b<r; b++){
                for (int a=0; a<r; a++){
                    for (int a=0; a<r; a++){

                    }
                }
            }
        }
        cin >> r >> c >> k;
    }
    return 0;
}
